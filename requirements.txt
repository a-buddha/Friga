# Python 3.10+ (frida-python compatibility; abi3 wheels also work on newer).
PyQt6>=6.6
PyQt6-WebEngine>=6.6        # hosts the Monaco script editor
PyQt6-QScintilla>=2.14      # fallback editor when QtWebEngine is unavailable
qtawesome>=1.3            # Font Awesome icons for the launcher UI (nav rail, title bar)
frida==17.12.0   # keep this matched to the bundled frida-server / frida-gadget
